[project]
name = "wolfgang-indian-tax-drafting"
version = "0.1.2"
description = "MCPB Desktop Extension for Indian tax drafting."
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Wolfgang Rush", email = "advrushikeshravindramahajan@gmail.com" }]
dependencies = ["mcp[cli]>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["server"]
